"""UK property growth research system (London FTB focus).

Modules:
  factors      - the factor taxonomy (single source of truth)
  scoring      - the tunable scoring engine
  config       - weight presets + loader
  sdlt         - first-time-buyer SDLT + scheme calculator
  datasources  - open-data catalog + runnable clients
  cli          - command-line interface
"""

__version__ = "1.0.0"
